import tkinter as tk
from tkinter import *
from matplotlib import font_manager


root = tk.Tk()
root.geometry("100x600")

# tekst iz kolone
text_label = Label(root, text="tekst iz kolone", font=("Arial", 16))
text_label.pack(pady=20)
def change_font(font_name):
    
    text_label.config(font=(font_name, 16))

def list_fonts():
    """List available fonts and create a button for each."""
    available_fonts = sorted([f.name for f in font_manager.fontManager.ttflist])
    
    # ovo je napravljeno za scrolanje niz fontove
    canvas = Canvas(root)
    frame = Frame(canvas)
    scrollbar = Scrollbar(root, orient="vertical", command=canvas.yview)
    canvas.configure(yscrollcommand=scrollbar.set)
    scrollbar.pack(side="right", fill="y")
    canvas.pack(side="left", fill="both", expand=True)
    canvas.create_window((0, 0), window=frame, anchor="nw")
    frame.bind("<Configure>", lambda event, canvas=canvas: canvas.configure(scrollregion=canvas.bbox("all")))

    for font in available_fonts:
        myButton = Button(frame, text=font, command=lambda f=font: change_font(f))
        myButton.pack()

# dugme za otvaranje liste fontova
font_button = Button(root, text="Change Fonts", command=list_fonts)
font_button.pack(pady=10)

root.mainloop()
